[package]
name = "sarge"
version = "4.0.1"
edition = "2021"

authors = ["Kyllingene"]
description = "Zero-dependencies command-line arguments parser."
repository = "https://github.com/kyllingene/sarge"

license = "MIT"

keywords = ["cli", "argparse", "command-line"]
categories = ["command-line-interface", ]

[dependencies]
# DO NOT add any dependencies!
# If you need a dependency for your addition,
# make your own crate (you can always fork).
